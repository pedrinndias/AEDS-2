#include<stdio.h>
int main(){

    int mat, idade;
    float peso;
    char sang;

    printf("Informe seua matricula, idade, peso e tipo sanguineo:");
    scanf("%d%d%f%[^n]", &mat, &idade, &peso, &sang);

    printf("Matricula")

    return 0;
}